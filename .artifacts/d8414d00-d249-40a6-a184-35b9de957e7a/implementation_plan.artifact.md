# Implementation Plan - Auto-Update Setup

This plan outlines how to set up an automated update system that delivers builds to users immediately after they are compiled (CI/CD).

## User Review Required

> [!IMPORTANT]
> The "after each build" requirement is best fulfilled via **GitHub Actions** for the independent version or **Google Play Internal Testing** for the store version.
>
> **Which platform are you targeting?**
> 1. **GitHub/Independent**: I will help you automate "Prereleases" that the app can detect.
> 2. **Google Play Store**: I will help you set up In-App Updates and CI automation for Play Store tracks.

## Proposed Changes

We will focus on the **GitHub-based approach** first as it's already partially implemented in your project and fits the "after each build" workflow perfectly.

### 1. CI/CD Automation (`.github/workflows`)

#### [MODIFY] [android.yml](file:///C:/Users/Avina/OneDrive/Documents/BoomingMusic-master/D2/BoomingMusic-master/.github/workflows/android.yml)
- Enhance the workflow to create or update a "Nightly" or "Continuous" GitHub Release on every push to the `main` branch.
- This release will contain the APKs generated by the build.

### 2. App Logic (In-App Updater)

#### [MODIFY] [GitHubService.kt](file:///C:/Users/Avina/OneDrive/Documents/BoomingMusic-master/D2/BoomingMusic-master/app/src/main/java/com/mardous/projectmusic/data/remote/github/GitHubService.kt)
- Ensure the version comparison logic correctly handles "development" or "CI" version strings (e.g., using the Git hash added in `build.gradle.kts`).

#### [MODIFY] [MainActivity.kt](file:///C:/Users/Avina/OneDrive/Documents/BoomingMusic-master/D2/BoomingMusic-master/app/src/main/java/com/mardous/projectmusic/ui/screen/MainActivity.kt)
- Optionally change the default behavior to check for "Experimental" updates if the user is using a CI build.

### 3. Build Configuration

#### [MODIFY] [build.gradle.kts](file:///C:/Users/Avina/OneDrive/Documents/BoomingMusic-master/D2/BoomingMusic-master/app/build.gradle.kts)
- Ensure the `versionName` includes a build identifier (like a timestamp or Git hash) for every CI build so the updater knows it's a new version.

## Verification Plan

### Automated Tests
- No specific automated tests, but we will verify the GitHub Action YAML syntax.

### Manual Verification
1. **Push to GitHub**: Observe the GitHub Action creating a new release.
2. **App Check**: Open the app and verify it detects the new "Nightly" build and prompts for update.
3. **Download**: Verify the APK downloads and initiates installation.
